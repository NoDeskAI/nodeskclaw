[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nanobot-security-layer"
version = "0.1.0"
description = "Tool execution security pipeline for nanobot (monkey-patch injector)"
requires-python = ">=3.11"
