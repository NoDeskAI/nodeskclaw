# ClawBuddy - 后端架构设计

> 与 PRD v0.4 配套
> 技术栈：Python 3.12 + FastAPI + kubernetes client + SQLAlchemy

---

## 一、分层架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         请求入口                                     │
│         HTTP (REST)  │  SSE 推送   │  Startup Events                │
└──────────┬───────────┴──────┬──────┴──────────┬─────────────────────┘
           │                  │                 │
           ▼                  ▼                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  API Layer (Routers)                                                 │
│  职责: 请求校验、路由分发、响应序列化                                   │
│                                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ auth.py  │ │clusters  │ │instances │ │ logs.py  │ │monitor   │ │
│  │          │ │ .py      │ │ .py      │ │          │ │ .py      │ │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ │
│       │            │            │            │            │         │
│  ┌────┴─────┐ ┌────┴──────┐                                       │
│  │deploy.py │ │stream.py  │                                        │
│  │          │ │ .py       │                                        │
│  └────┬─────┘ └────┬──────┘                                        │
└───────┼─────────────┼──────────────────────────────────────────────┘
        │             │
        ▼             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Service Layer (业务逻辑)                                            │
│  职责: 核心业务、编排、状态管理                                        │
│                                                                     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                │
│  │ AuthService  │ │ClusterService│ │InstanceSvc   │                │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘                │
│  ┌──────┴───────┐ ┌──────┴───────┐ ┌──────┴───────┐                │
│  │DeployService │ │ LogService   │ │MonitorService│                │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘                │
│         │                │                │                         │
└─────────┼────────────────┼────────────────┼─────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Infrastructure Layer (基础设施)                                      │
│  职责: 外部系统交互、数据持久化、实时通信                                │
│                                                                     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                │
│  │ K8sClient    │ │   Database   │ │  Broadcaster │                │
│  │ (k8s-asyncio)│ │ (SQLAlchemy) │ │ (SSE 推送)   │                │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘                │
│  ┌──────┴───────┐ ┌──────┴───────┐ ┌──────┴───────┐                │
│  │ResourceBuilder│ │  K8sWatcher  │ │  TaskQueue   │                │
│  │ (资源构建器)  │ │  (Watch API) │ │  (asyncio)   │                │
│  └──────────────┘ └──────────────┘ └──────────────┘                │
└─────────────────────────────────────────────────────────────────────┘
          │                │
          ▼                ▼
   K8s API Server    PostgreSQL (火山云 RDS)
```

---

## 二、目录结构

```
claw-buddy-backend/
├── app/
│   ├── __init__.py
│   ├── main.py                    # FastAPI 入口，挂载路由、中间件、生命周期
│   │
│   ├── api/                       # === API Layer ===
│   │   ├── __init__.py
│   │   ├── router.py              # 汇总所有路由 prefix
│   │   ├── auth.py                # 认证：飞书 SSO 回调、Token 签发/刷新
│   │   ├── clusters.py            # 集群：CRUD、连接测试、概览
│   │   ├── instances.py           # 实例：列表、详情、操作（重启/扩缩容/删除）
│   │   ├── deploy.py              # 部署：预检、创建部署、部署进度查询
│   │   ├── llm_keys.py            # LLM Key 管理：组织 Key CRUD、用户 Key CRUD、用户 LLM 配置、模型列表
│   │   ├── llm_proxy.py           # LLM 代理：流式转发 OpenClaw 的 LLM 请求（挂在 app 根路径）
│   │   ├── logs.py                # 日志：历史日志查询
│   │   ├── monitor.py             # 监控：Pod 状态、Metrics、Events、部署历史
│   │   └── stream.py              # SSE 流端点：日志流、事件流、部署进度、状态推送
│   │
│   ├── services/                  # === Service Layer ===
│   │   ├── __init__.py
│   │   ├── auth_service.py        # 飞书 OAuth、JWT 签发/校验
│   │   ├── cluster_service.py     # 集群管理、KubeConfig 解析/加密/存储
│   │   ├── instance_service.py    # 实例 CRUD、状态聚合
│   │   ├── deploy_service.py      # 部署编排：预检 → 创建 K8s 资源 → 状态轮询
│   │   ├── llm_config_service.py  # LLM 配置读写 OpenClaw（通过 NFS 临时挂载读写 openclaw.json）
│   │   ├── model_catalog_service.py # 模型目录：从各 Provider API 拉取可用模型列表（内存缓存）
│   │   ├── nfs_mount.py           # NFS 临时挂载管理（PV 自动发现、mount/umount）
│   │   ├── openclaw_session.py    # OpenClaw 会话/配置文件操作（NFS 挂载后的读写层）
│   │   ├── resource_builder.py    # K8s 资源构建器（ConfigMap/Deployment/Service/Ingress）
│   │   ├── log_service.py         # Pod 日志读取（K8s API）
│   │   └── monitor_service.py     # Metrics/Events/History 聚合
│   │
│   ├── realtime/                  # === 实时通信 ===
│   │   ├── __init__.py
│   │   ├── event_bus.py            # SSE 事件总线（内存 pub/sub，多连接广播）
│   │   ├── k8s_watcher.py         # K8s Watch API：Pod 状态、Events 变更监听
│   │   └── deploy_tracker.py      # 部署进度跟踪（轮询 Deployment 状态）
│   │
│   ├── models/                    # === 数据模型 (SQLAlchemy) ===
│   │   ├── __init__.py
│   │   ├── base.py                # Base, 通用字段（id, created_at, updated_at, deleted_at）
│   │   ├── user.py                # User 表
│   │   ├── cluster.py             # Cluster 表
│   │   ├── instance.py            # Instance (OpenClaw 部署实例) 表
│   │   ├── deploy_record.py       # DeployRecord (部署历史) 表
│   │   ├── org_llm_key.py         # OrgLlmKey (组织 LLM Key) 表
│   │   ├── user_llm_key.py        # UserLlmKey (用户个人 LLM Key) 表
│   │   ├── user_llm_config.py     # UserLlmConfig (用户 LLM Key 选择配置 + 模型选择) 表
│   │   ├── llm_usage_log.py       # LlmUsageLog (LLM 用量记录，追加表) 表
│   │   └── audit_log.py           # AuditLog (操作审计) 表
│   │
│   ├── schemas/                   # === Pydantic 模型 ===
│   │   ├── __init__.py
│   │   ├── common.py              # 统一响应 ApiResponse、分页 Pagination
│   │   ├── auth.py                # LoginRequest, TokenResponse, UserInfo
│   │   ├── cluster.py             # ClusterCreate, ClusterInfo, ClusterOverview
│   │   ├── instance.py            # InstanceCreate, InstanceInfo, InstanceDetail
│   │   ├── deploy.py              # DeployRequest, DeployPrecheck, DeployProgress
│   │   ├── log.py                 # LogQuery, LogLine
│   │   ├── monitor.py             # PodStatus, Metrics, K8sEvent, DeployHistory
│   │   └── stream.py              # SSE 事件类型定义（event name + data schema）
│   │
│   ├── core/                      # === 核心配置 ===
│   │   ├── __init__.py
│   │   ├── config.py              # Settings (pydantic-settings)
│   │   ├── security.py            # JWT 工具、密码加密、KubeConfig 加密
│   │   ├── deps.py                # 依赖注入：get_db, get_current_user, get_k8s_client
│   │   ├── exceptions.py          # 自定义异常 + 全局异常处理器
│   │   └── middleware.py          # 请求日志、CORS、审计中间件
│   │
│   └── utils/                     # === 工具 ===
│       ├── __init__.py
│       ├── k8s_helpers.py         # K8s 资源格式转换、状态判断
│       ├── crypto.py              # AES 加密/解密（KubeConfig 加密存储）
│       └── feishu.py              # 飞书 API 封装（SSO、消息推送）
│
├── logs/                          # 滚动日志（.gitignore 已排除）
│   ├── clawbuddy.log             # 当前日志文件
│   └── clawbuddy.log.1~5         # 历史滚动文件
│
├── tests/                         # 测试
│   ├── conftest.py                # fixtures: test_client, test_db, mock_k8s
│   ├── api/                       # API 层测试
│   ├── services/                  # Service 层测试
│   └── realtime/                  # SSE 流测试
│
├── Dockerfile
└── pyproject.toml
```

---

## 三、数据模型（ER 图）

```
┌─────────────────────┐       ┌──────────────────────────┐
│       User          │       │        Cluster           │
├─────────────────────┤       ├──────────────────────────┤
│ id          PK      │       │ id              PK       │
│ feishu_uid  UNIQUE  │       │ name            UNIQUE   │
│ name                │       │ provider        enum     │  ← VKE / ACK / TKE
│ email               │       │ kubeconfig_enc  TEXT     │  ← AES 加密存储
│ avatar_url          │       │ auth_type       VARCHAR  │  ← certificate/token/exec
│ role        enum    │──┐    │ token_expires_at DATETIME│  ← Token 模式过期时间
│ is_active           │  │    │ api_server_url           │
│ last_login_at       │  │    │ k8s_version              │
│ created_at          │  │    │ status          enum     │  ← connected / disconnected
│ updated_at          │  │    │ health_status   VARCHAR  │  ← healthy/unhealthy/token_expired
│ deleted_at  DATETIME│  │    │ last_health_check DATETIME│
└─────────────────────┘  │    │ created_by      FK→User  │
                         │    │ created_at               │
  * deleted_at = NULL    │    │ updated_at               │
    表示正常记录         │    │ deleted_at      DATETIME │  ← NULL=正常, 有值=已删除
  * deleted_at 有值      │    └───────────┬──────────────┘
    表示已逻辑删除       │                │
                         │                │
         role:           │                │ 1:N
         admin / user    │                │
                         │    ┌───────────▼──────────────┐
                         │    │       Instance            │
                         │    ├──────────────────────────┤
                         │    │ id              PK       │
                         │    │ name            UNIQUE   │
                         │    │ cluster_id      FK→Cluster│
                         │    │ namespace                │
                         │    │ image_version             │
                         │    │ replicas                 │
                         │    │ cpu_request              │
                         │    │ cpu_limit                │
                         │    │ mem_request              │
                         │    │ mem_limit                │
                         │    │ service_type    enum     │  ← ClusterIP / NodePort / LB
                         │    │ ingress_domain           │
                         │    │ env_vars        JSON     │  ← {"OPENCLAW_MODEL_PROVIDER":"openai",...}
                         │    │ quota_cpu       VARCHAR  │  ← Namespace 配额: "4" (核)
                         │    │ quota_mem       VARCHAR  │  ← Namespace 配额: "8Gi"
                         │    │ quota_max_pods  INT      │  ← Namespace 配额: 20
                         │    │ storage_class   VARCHAR  │  ← StorageClass: "nas-subpath" (默认)
                         │    │ storage_size    VARCHAR  │  ← 存储空间: "10Gi" (默认)
                         │    │ advanced_config JSON     │  ← 高级配置 (Volume/Sidecar/网络)
                         │    │ status          enum     │  ← creating/pending/running/
                         │    │                          │     updating/failed/deleting
                         │    │ current_revision INT     │  ← 部署版本号（自增）
                         │    │ created_by      FK→User  │
                         └────│ created_at               │
                              │ updated_at               │
                              │ deleted_at      DATETIME │  ← NULL=正常, 有值=已删除
                              └───────────┬──────────────┘
                                          │
                                          │ 1:N
                                          │
                              ┌───────────▼──────────────┐
                              │     DeployRecord         │
                              ├──────────────────────────┤
                              │ id              PK       │
                              │ instance_id     FK→Inst  │
                              │ revision        INT      │
                              │ action          enum     │  ← create / upgrade / rollback
                              │ image_version            │     / scale / restart / delete
                              │ replicas                 │
                              │ config_snapshot JSON     │  ← 完整配置快照
                              │ status          enum     │  ← success / failed / in_progress
                              │ message                  │  ← 失败原因 / 变更说明
                              │ triggered_by    FK→User  │
                              │ started_at               │
                              │ finished_at              │
                              │ created_at               │
                              │ deleted_at      DATETIME │  ← NULL=正常, 有值=已删除
                              └──────────────────────────┘

┌──────────────────────────┐
│       AuditLog           │
├──────────────────────────┤
│ id              PK       │
│ user_id         FK→User  │
│ action          VARCHAR  │  ← "instance.create" / "cluster.delete" / ...
│ resource_type   VARCHAR  │  ← "instance" / "cluster"
│ resource_id     VARCHAR  │
│ resource_name   VARCHAR  │
│ detail          JSON     │  ← 操作详情 {"old": {...}, "new": {...}}
│ ip_address               │
│ user_agent               │
│ created_at               │
└──────────────────────────┘
```

### LLM 代理相关模型

```
┌──────────────────────────┐         ┌──────────────────────────┐
│       OrgLlmKey          │         │       UserLlmKey         │
├──────────────────────────┤         ├──────────────────────────┤
│ id              PK       │         │ id              PK       │
│ org_id     FK→Organization│        │ user_id    FK→User       │
│ provider      VARCHAR(32)│         │ provider      VARCHAR(32)│
│ label        VARCHAR(128)│         │ api_key         TEXT     │  ← 明文（初期）
│ api_key         TEXT     │         │ base_url   VARCHAR(512)  │  ← 可选
│ base_url   VARCHAR(512)  │         │ is_active       BOOLEAN  │
│ org_token_limit  BIGINT  │  ← NULL=不限│ created_at / updated_at │
│ system_token_limit BIGINT│  ← NULL=不限│ deleted_at              │
│ is_active       BOOLEAN  │         └──────────────────────────┘
│ created_by  FK→User      │         唯一约束: (user_id, provider)
│ created_at / updated_at  │                   WHERE deleted_at IS NULL
│ deleted_at               │
└───────────┬──────────────┘
            │ 无唯一约束 — 同组织同 Provider 可多 Key
            │
            │ 1:N
            ▼
┌──────────────────────────┐
│     UserLlmConfig        │         ┌──────────────────────────┐
├──────────────────────────┤         │      LlmUsageLog         │
│ id              PK       │         ├──────────────────────────┤
│ user_id    FK→User       │         │ id              PK       │
│ org_id     FK→Organization│        │ org_llm_key_id FK(可空)  │  ← 个人 Key 时 NULL
│ provider      VARCHAR(32)│         │ user_id    FK→User       │
│ key_source   VARCHAR(16) │  ← org/personal│ instance_id FK→Instance│
│ org_llm_key_id FK(可空)  │         │ provider      VARCHAR(32)│
│ created_at / updated_at  │         │ model       VARCHAR(64)  │
│ deleted_at               │         │ prompt_tokens     INT    │
└──────────────────────────┘         │ completion_tokens INT    │
唯一约束: (user_id, org_id, provider)│ total_tokens      INT    │
           WHERE deleted_at IS NULL  │ created_at    TIMESTAMP  │
                                     └──────────────────────────┘
                                     纯追加表，不继承 BaseModel
                                     索引: org_llm_key_id, (user_id, created_at)
```

Instance 表新增字段：
- `proxy_token` VARCHAR(64)，唯一索引，与 `OPENCLAW_GATEWAY_TOKEN` 同值。用于 OpenClaw Control UI / WebSocket 网关认证。
- `wp_api_key` VARCHAR(96)，唯一索引，格式 `clawbuddy-wp-{64位hex}`。用于 LLM Proxy 认证，与 gateway token 分离。

### 软删除策略

所有模型均通过 `BaseModel` 继承 `deleted_at` 字段，采用逻辑删除：

- **删除操作**：设置 `deleted_at = now()`，不物理删除行
- **查询过滤**：所有 `select()` 查询必须加 `Model.deleted_at.is_(None)` 条件
- **级联逻辑删除**：删除集群时级联逻辑删除其下实例和部署记录；删除实例时级联逻辑删除部署记录
- **名称复用**：已逻辑删除的记录不占用名称唯一性，同名可重新创建
- **自动迁移**：升级到软删除版本后首次启动时自动为已有表添加 `deleted_at` 列和索引

### 枚举值定义

```python
class UserRole(str, Enum):
    admin = "admin"
    user = "user"

class ClusterProvider(str, Enum):
    vke = "vke"         # 火山云
    ack = "ack"         # 阿里云 (预留)
    tke = "tke"         # 腾讯云 (预留)
    custom = "custom"   # 自建集群

class ClusterStatus(str, Enum):
    connected = "connected"
    disconnected = "disconnected"
    connecting = "connecting"

class InstanceStatus(str, Enum):
    creating = "creating"
    pending = "pending"
    running = "running"
    updating = "updating"
    failed = "failed"
    deleting = "deleting"

class ServiceType(str, Enum):
    cluster_ip = "ClusterIP"
    node_port = "NodePort"
    load_balancer = "LoadBalancer"

class DeployAction(str, Enum):
    create = "create"
    upgrade = "upgrade"
    rollback = "rollback"
    scale = "scale"
    restart = "restart"
    delete = "delete"

class DeployStatus(str, Enum):
    in_progress = "in_progress"
    success = "success"
    failed = "failed"
```

---

## 四、完整 API 设计

### 4.1 通用约定

**Base URL**: `/api/v1`

**统一响应格式**:

```python
class ApiResponse(BaseModel, Generic[T]):
    code: int = 0              # 0=成功, 非0=错误码
    message: str = "success"
    data: Optional[T] = None

class PageResponse(ApiResponse[T]):
    total: int = 0
    page: int = 1
    page_size: int = 20
```

**错误码规范**:

| 范围 | 模块 | 示例 |
|------|------|------|
| 10xxx | 认证 | 10001 Token 过期, 10002 无权限 |
| 20xxx | 集群 | 20001 连接失败, 20002 KubeConfig 无效 |
| 30xxx | 实例 | 30001 名称已存在, 30002 实例不存在 |
| 40xxx | 部署 | 40001 预检失败, 40002 资源创建错误 |
| 50xxx | 系统 | 50001 内部错误 |

### 4.2 认证 `/api/v1/auth`

```
GET    /health                    ClawBuddy 健康检查（K8s liveness/readiness probe）

POST   /auth/feishu/callback     飞书 SSO 回调，换取 JWT
POST   /auth/refresh              刷新 Token
GET    /auth/me                   获取当前用户信息
POST   /auth/logout               登出（废弃 Token）
```

| 方法 | 路径 | 请求体 | 响应 data | 说明 |
|------|------|--------|-----------|------|
| POST | `/auth/feishu/callback` | `{ code: str }` | `{ access_token, refresh_token, user }` | 飞书临时码换 JWT |
| POST | `/auth/refresh` | `{ refresh_token: str }` | `{ access_token, refresh_token }` | 刷新 Token |
| GET | `/auth/me` | — | `UserInfo` | 当前用户信息 |
| POST | `/auth/logout` | — | — | 登出 |

### 4.3 集群管理 `/api/v1/clusters`

```
GET    /clusters                  集群列表
POST   /clusters                  添加集群
GET    /clusters/:id              集群详情
PUT    /clusters/:id              更新集群配置
DELETE /clusters/:id              删除集群
POST   /clusters/:id/test         测试集群连接
GET    /clusters/:id/overview     集群概览（节点、资源）
GET    /clusters/:id/nodes        节点列表
GET    /clusters/:id/namespaces   命名空间列表
GET    /clusters/:id/health       集群健康状态 + Token 有效期
POST   /clusters/:id/kubeconfig   更新 KubeConfig（重建连接）
```

| 方法 | 路径 | 请求体 | 响应 data | 说明 |
|------|------|--------|-----------|------|
| GET | `/clusters` | — | `ClusterInfo[]` | 集群列表 |
| POST | `/clusters` | `ClusterCreate` | `ClusterInfo` | 添加集群 |
| GET | `/clusters/:id` | — | `ClusterInfo` | 集群详情 |
| PUT | `/clusters/:id` | `ClusterUpdate` | `ClusterInfo` | 更新配置 |
| DELETE | `/clusters/:id` | — | — | 删除集群 |
| POST | `/clusters/:id/test` | — | `{ ok, version, nodes }` | 连接测试 |
| GET | `/clusters/:id/overview` | — | `ClusterOverview` | 概览数据 |
| GET | `/clusters/:id/nodes` | — | `NodeInfo[]` | 节点列表 |
| GET | `/clusters/:id/namespaces` | — | `string[]` | 命名空间列表 |
| GET | `/clusters/:id/health` | — | `ClusterHealth` | 健康状态 + Token 有效期 |
| POST | `/clusters/:id/kubeconfig` | `{ kubeconfig: str }` | `ClusterInfo` | 更新 KubeConfig（重建连接） |

**关键 Schema**:

```python
class ClusterCreate(BaseModel):
    name: str
    provider: ClusterProvider = ClusterProvider.vke
    kubeconfig: str              # 明文传入，后端 AES 加密后存储
    description: Optional[str]

class ClusterOverview(BaseModel):
    name: str
    version: str
    node_count: int
    node_ready: int
    cpu_total: str               # "20c"
    cpu_used: str                # "12c"
    cpu_percent: float           # 60.0
    memory_total: str            # "40Gi"
    memory_used: str             # "24Gi"
    memory_percent: float
    pod_count: int

class NodeInfo(BaseModel):
    name: str
    status: str                  # Ready / NotReady
    ip: str
    cpu_capacity: str
    cpu_used: str
    memory_capacity: str
    memory_used: str
    pod_count: int
    conditions: list[dict]

class ClusterHealth(BaseModel):
    auth_type: str               # certificate / token / exec
    token_status: str            # ok / expiring_soon / expired / not_token
    token_expires_at: Optional[str]  # ISO 时间
    healthy: bool
    last_health_check: Optional[str]
```

### 4.4 实例管理 `/api/v1/instances`

```
GET    /instances                  实例列表（支持筛选、分页）
GET    /instances/:id              实例详情
PUT    /instances/:id              修改实例配置
DELETE /instances/:id              删除实例
POST   /instances/:id/restart      重启
POST   /instances/:id/scale        扩缩容（M1 锁定单副本，预留接口）
GET    /instances/:id/pods         Pod 列表
```

| 方法 | 路径 | 请求体/参数 | 响应 data | 说明 |
|------|------|------------|-----------|------|
| GET | `/instances` | `?status=&ns=&page=&size=` | `PageResponse[InstanceInfo]` | 列表 |
| GET | `/instances/:id` | — | `InstanceDetail` | 详情（含 Pod 列表） |
| PUT | `/instances/:id` | `InstanceUpdate` | `InstanceInfo` | 修改配置 |
| DELETE | `/instances/:id` | — | — | 删除（清理 K8s 资源） |
| POST | `/instances/:id/restart` | `{ strategy: "rolling"\|"recreate" }` | `DeployRecord` | 重启 |
| POST | `/instances/:id/scale` | `{ replicas: int }` | `InstanceInfo` | 扩缩容 |
| GET | `/instances/:id/pods` | — | `PodInfo[]` | Pod 列表 |
| GET | `/instances/:id/advanced-config` | — | `AdvancedConfig` | 获取高级配置 |
| PUT | `/instances/:id/advanced-config` | `AdvancedConfig` | `AdvancedConfig` | 更新高级配置 |
| POST | `/instances/:id/advanced-config/apply` | — | `DeployRecord` | 应用高级配置到 K8s |

> **高级配置更新流程**：先通过 PUT 保存到 DB，再通过 POST apply 应用到 K8s（两步操作，避免误修改直接生效）。

**关键 Schema**:

```python
class InstanceInfo(BaseModel):
    id: str
    name: str
    slug: str
    org_id: Optional[str]         # 所属组织 ID（Portal 设置页用于获取组织级 Key）
    cluster_id: str
    namespace: str
    image_version: str
    replicas: int
    available_replicas: int       # 来自 K8s 实时查询
    status: InstanceStatus
    service_type: ServiceType
    ingress_domain: Optional[str]
    created_by: str
    created_at: datetime
    updated_at: datetime

class InstanceDetail(InstanceInfo):
    cpu_request: str
    cpu_limit: str
    mem_request: str
    mem_limit: str
    env_vars: dict[str, str]
    pods: list[PodInfo]           # 实时 Pod 信息
    service_info: ServiceInfo
    events: list[K8sEvent]        # 最近 20 条事件

class PodInfo(BaseModel):
    name: str
    status: str                   # Running / Pending / Failed / ...
    node: str
    ip: str
    cpu_used: Optional[str]       # 来自 Metrics API
    memory_used: Optional[str]
    restart_count: int
    started_at: Optional[datetime]
    containers: list[ContainerInfo]
```

### 4.5 部署 `/api/v1/deploy`

```
POST   /deploy/precheck            部署预检
POST   /deploy                     执行部署
GET    /deploy/:id                 部署进度查询
POST   /deploy/:id/cancel          取消部署
GET    /deploy/image-tags          可用镜像版本列表
```

| 方法 | 路径 | 请求体 | 响应 data | 说明 |
|------|------|--------|-----------|------|
| POST | `/deploy/precheck` | `DeployRequest` | `PrecheckResult` | 预检 |
| POST | `/deploy` | `DeployRequest` | `DeployRecord` | 创建部署（异步） |
| GET | `/deploy/:id` | — | `DeployProgress` | 部署进度 |
| POST | `/deploy/:id/cancel` | — | — | 取消 |
| GET | `/deploy/image-tags` | `?registry=` | `ImageTag[]` | 镜像列表 |

**关键 Schema**:

```python
class DeployRequest(BaseModel):
    cluster_id: str
    name: str                                # 实例名，Namespace 自动生成为 clawbuddy-{name}
    image_version: str
    replicas: int = 1                        # 固定为 1，前端只读
    cpu_request: str = "500m"
    cpu_limit: str = "2000m"
    mem_request: str = "2Gi"
    mem_limit: str = "2Gi"
    env_vars: dict[str, str] = {}
    # --- Namespace 资源配额（隔离策略）---
    quota_cpu: str = "4"                     # Namespace CPU 配额上限
    quota_mem: str = "8Gi"                   # Namespace 内存配额上限
    quota_max_pods: int = 20                 # Namespace 最大 Pod 数
    storage_class: str = "nas-subpath"        # 用户选择的 StorageClass（部署时可选）
    storage_size: str = "10Gi"               # 实例持久化存储大小，PVC 挂载到 /root
    # --- 以下字段由后端自动处理，前端无需传入 ---
    # service_type: 固定为 ClusterIP
    # ingress_domain: 自动生成为 {name}.{ingress_base_domain}（从 DB 系统配置读取）

class PrecheckResult(BaseModel):
    passed: bool
    checks: list[PrecheckItem]

class PrecheckItem(BaseModel):
    name: str                     # "cluster_connection" / "namespace" / ...
    status: str                   # "pass" / "fail" / "warn"
    message: str

class DeployProgress(BaseModel):
    deploy_id: str
    status: DeployStatus          # in_progress / success / failed
    progress: int                 # 0-100
    current_step: str             # "creating_deployment" / "waiting_pods" / ...
    steps: list[DeployStep]
    logs: list[str]               # 部署日志行

class DeployStep(BaseModel):
    name: str
    status: str                   # "done" / "running" / "pending" / "failed"
    message: Optional[str]
```

### 4.6 日志 `/api/v1/logs`

```
GET    /logs/:namespace/:pod       历史日志（REST，分页）
```

| 方法 | 路径 | 参数 | 响应 data | 说明 |
|------|------|------|-----------|------|
| GET | `/logs/:ns/:pod` | `?container=&lines=500&since=` | `LogResult` | 历史日志 |

> 实时日志通过 SSE `GET /stream/instances/:id/logs` 提供，见第六节。

### 4.7 监控 `/api/v1/monitor`

```
GET    /monitor/instances/:id/metrics    资源指标（CPU/内存曲线数据）
GET    /monitor/instances/:id/events     K8s Events
GET    /monitor/instances/:id/history    部署历史 (Revision 列表)
POST   /monitor/instances/:id/rollback   版本回滚
```

| 方法 | 路径 | 参数/请求体 | 响应 data | 说明 |
|------|------|------------|-----------|------|
| GET | `/.../metrics` | `?range=1h\|6h\|24h\|7d` | `MetricsData` | 资源曲线 |
| GET | `/.../events` | `?type=Normal\|Warning&limit=50` | `K8sEvent[]` | 事件列表 |
| GET | `/.../history` | — | `DeployRecord[]` | 部署历史 |
| POST | `/.../rollback` | `{ revision: int }` | `DeployRecord` | 回滚 |

### 4.8 LLM Key 管理 `/api/v1/orgs` + `/api/v1/users`

#### 双通道模型

LLM Key 使用分为两种模式：

- **组织 Key（proxy 模式）**：用户选择后，请求经由 ClawBuddy 的 `llm_proxy` 转发。Proxy 自动按 `(org_id, provider)` 查找管理员配置的 `org_llm_keys` 中首条可用 Key 进行调用。用户无需选择具体哪条 Key。`openclaw.json` 写入 proxy URL + `wp_api_key`（独立于 gateway token 的 LLM Proxy 专用认证 Key）。
- **个人 Key（直连模式）**：用户提供自己的 API Key，请求直接发往目标 Provider 的 base URL，不经过 proxy。`openclaw.json` 直接写入用户真实 API Key + Provider 默认 base URL。

**组织 Key 管理（Admin 平台）**：

```
GET    /orgs/:org_id/llm-keys                列出组织所有 LLM Key（api_key 脱敏）
POST   /orgs/:org_id/llm-keys                创建 Key
PATCH  /orgs/:org_id/llm-keys/:key_id        更新 Key
DELETE /orgs/:org_id/llm-keys/:key_id        软删除 Key
GET    /orgs/:org_id/available-llm-keys      可选 Key 列表（供查询用）
```

| 方法 | 路径 | 请求体 | 响应 data | 说明 |
|------|------|--------|-----------|------|
| GET | `/orgs/:id/llm-keys` | — | `OrgLlmKey[]`（含 SUM(usage)） | 脱敏展示 |
| POST | `/orgs/:id/llm-keys` | `{ provider, label, api_key, base_url?, org_token_limit?, system_token_limit? }` | `OrgLlmKey` | 创建 |
| PATCH | `/orgs/:id/llm-keys/:kid` | `{ label?, api_key?, ... }` | `OrgLlmKey` | 更新 |
| DELETE | `/orgs/:id/llm-keys/:kid` | — | — | 软删除 |

**用户个人 Key 管理（Portal）**：

```
GET    /users/me/llm-keys                    列出个人 Key（脱敏）
POST   /users/me/llm-keys                    创建/更新（upsert by provider）
DELETE /users/me/llm-keys/:provider          删除
```

**用户 LLM 配置（Portal -- Key 来源选择）**：

```
GET    /users/me/llm-configs?org_id=xxx      获取用户在指定组织的配置
PUT    /users/me/llm-configs                 批量更新
```

| 方法 | 路径 | 请求体 | 响应 data | 说明 |
|------|------|--------|-----------|------|
| PUT | `/users/me/llm-configs` | `{ org_id, configs: [{provider, key_source, selected_models?}] }` | `{ needs_restart, affected_instances[] }` | 保存并检测是否需要重启。`key_source` 为 `org` 或 `personal`。`selected_models` 为用户选择的模型数组（可选），自定义 Provider 必填 |

**模型目录（Provider 可用模型列表）**：

```
GET    /llm/providers/:provider/models?api_key=&org_id=   拉取指定 Provider 的可用模型列表
```

从各 Provider 官方 API 拉取模型列表，支持 OpenAI / Anthropic / Gemini / OpenRouter / Minimax。内存缓存 10 分钟。优先使用传入的 `api_key`（个人 Key 场景），否则按 `org_id` + `provider` 查找组织 Key。返回 `{ provider, models: [{id, name, context_window, max_tokens}] }`。

**实例 LLM 状态 + 重启**：

```
GET    /instances/:id/openclaw-providers     通过 NFS 临时挂载读取 openclaw.json 中的 Provider 配置
GET    /instances/:id/llm-config             生效 LLM 配置（只读，从 DB 读取）
POST   /instances/:id/restart-openclaw       优雅重启 OpenClaw 进程
```

| 方法 | 路径 | 响应 data | 说明 |
|------|------|-----------|------|
| GET | `/instances/:id/openclaw-providers` | `{ data_source, providers: [{ provider, base_url, is_proxy, key_source, api_key_masked }] }` | 通过 NFS 临时挂载读取 PVC 上的 openclaw.json（不依赖 Pod 运行状态），交叉查询 DB 补充 Key 来源信息。NFS 地址从 PV spec 自动发现，无需配置 |

### 4.9 LLM 代理（独立服务 `claw-buddy-llm-proxy`）

LLM 代理已拆分为独立项目 `claw-buddy-llm-proxy/`，独立部署到 K8s，通过私网域名供 OpenClaw 实例访问。后端 `claw-buddy-backend` 中的 `llm_proxy.py` 仅在 `DEBUG=true` 时挂载，用于本地开发。

```
ANY    /:provider/*path            LLM 请求代理转发（仅组织 Key 模式使用）
```

使用 `instances.wp_api_key` 鉴权（独立于 gateway token），不依赖 JWT。兼容过渡期：若 `wp_api_key` 未匹配到实例，回退查 `proxy_token`。

流程：wp_api_key 查实例 -> user_llm_configs 查 Key 来源 -> 按 `(org_id, provider)` 自动查找首条可用 org_llm_key -> 额度检查 -> 流式转发 -> 记录 usage。

个人 Key 模式下请求不经过此代理，由 OpenClaw 直连目标 Provider。

支持的 Provider：OpenAI、Anthropic、Gemini、OpenRouter、Minimax-OpenAI、Minimax-Anthropic。认证头按类型区分：OpenAI/OpenRouter/Minimax 用 `Authorization: Bearer`，Anthropic 用 `x-api-key`，Gemini 用 URL 参数。

**部署架构**：LLM Proxy 以 FastAPI 服务运行在 K8s Pod 中，同 Pod 内运行 Clash sidecar 提供出站代理（访问 OpenAI/Anthropic 等外部 API）。通过私网 DNS 解析使 10 网段内的 OpenClaw Pod 可通过域名直达。后端写入 `openclaw.json` 时，组织 Key 的 `baseUrl` 指向 `{LLM_PROXY_URL}/{provider}/v1`。详细部署方案见 `docs/VKE部署方案.md` 第十节。

---

## 五、Service 层核心逻辑

### 5.1 DeployService — 部署编排

整个部署流程是异步的，通过 `asyncio.create_task` 启动后台任务，前端通过 SSE `GET /stream/deploy/:id/progress` 接收进度。

```
DeployService.create_deploy(request) 流程:

  1. 参数校验 & 入库 (DeployRecord, status=in_progress)
  2. asyncio.create_task → _do_deploy(deploy_id)
  3. 立即返回 deploy_id 给前端

_do_deploy(deploy_id):
  │
  ├─ Step 1: 创建 Namespace + 隔离策略
  │  ├─ 创建 Namespace clawbuddy-{name}（带管理标签，名称 RFC 1123 清洗）
  │  ├─ 创建 ResourceQuota（CPU/内存/Pod 数/存储上限）
  │  ├─ 创建 LimitRange（容器默认 limits 兜底）
  │  ├─ 创建 NetworkPolicy（网络隔离：禁止跨实例访问）
  │  ├─ 创建 PVC {name}-root-data（用户选择的 StorageClass，默认 nas-subpath，挂载到 /root）
  │  └─ broadcast("deploy:{id}", {step: "namespace", status: "done"})
  │
  ├─ Step 2: 创建 K8s 资源（ConfigMap + Deployment + Service(ClusterIP) + Ingress(自动路由)）
  │  ├─ 调用 create_openclaw_instance(k8s, request, instance_id)
  │  │   ├─ kubernetes-asyncio 直接创建各资源
  │  │   ├─ Service 固定 ClusterIP，端口 18789
  │  │   └─ Ingress 自动生成 hostname: {name}.{ingress_base_domain}，含 TLS + WebSocket annotations
  │  └─ broadcast("deploy:{id}", {step: "create_resources", status: "done"})
  │
  ├─ Step 3: 轮询 Deployment 状态（最多 5 分钟）
  │  ├─ while not all_pods_ready:
  │  │   ├─ 查询 Deployment .status
  │  │   ├─ 计算 progress 百分比
  │  │   ├─ broadcast("deploy:{id}", {progress, pods_ready, pods_total})
  │  │   └─ await asyncio.sleep(3)
  │  └─ broadcast("deploy:{id}", {step: "pods_ready", status: "done"})
  │
  ├─ Step 4: 验证 Service / Ingress 可达
  │  └─ broadcast("deploy:{id}", {step: "service_ready", status: "done"})
  │
  ├─ 成功: 更新 DeployRecord.status = success, Instance.status = running
  │  └─ broadcast("deploy:{id}", {status: "success", progress: 100})
  │
  └─ 失败: 更新 DeployRecord.status = failed, Instance.status = failed
     └─ broadcast("deploy:{id}", {status: "failed", error: message})
```

### 后台任务 Session 管理规范

通过 `asyncio.create_task()` 启动的后台任务 **不能** 复用请求作用域的 `db: AsyncSession`，因为请求结束后 FastAPI 的 `get_db` 依赖会关闭该 session。后台任务必须在内部通过 `async_session_factory()` 创建独立 session，并通过 ID 重新加载需要操作的数据库对象。

```python
# 正确：后台任务内部创建独立 session
async def _background_task(record_id: str) -> None:
    from app.core.deps import async_session_factory
    async with async_session_factory() as db:
        record = await db.get(Model, record_id)
        # ... 操作 ...
        await db.commit()

# 调用处只传 ID
asyncio.create_task(_background_task(record.id))
```

涉及此模式的模块：`deploy_service.py`（部署编排）、`gene_service.py`（基因安装/学习任务）。

### Custom Skill 发现机制

OpenClaw 的 skill 加载分为两类：

- **Native skills**：内置于 OpenClaw 安装目录（`/usr/local/lib/node_modules/openclaw/skills/`），通过 `commands.nativeSkills: "auto"` 自动发现
- **Custom skills**：由基因安装写入 `/root/.openclaw/skills/<skill-name>/SKILL.md`，需要在 `openclaw.json` 中通过 `skills.load.extraDirs` 显式指定扫描目录

基因安装流程（`_direct_install`）和加入工作区流程（`deploy_clawbuddy_channel_plugin`）都会调用 `openclaw_session.ensure_skills_discovery()` 确保 `openclaw.json` 包含：

```json
{
  "skills": {
    "load": {
      "extraDirs": ["/root/.openclaw/skills"]
    }
  }
}
```

使用绝对路径 `/root/.openclaw/skills` 而非相对路径，因为 OpenClaw 会将相对路径解析为相对于 workspace 目录（`/root/.openclaw/workspace/`），导致路径错误。

**SKILL.md 格式要求**：OpenClaw 的 `loadSkillFromFile` 要求 SKILL.md 必须包含 YAML front matter 中的 `name` 和 `description` 字段，否则 skill 不会被加载。`_write_skill_file` 会在内容缺少 front matter 时自动补全：

```yaml
---
name: <skill-name>
description: <gene.short_description>
---
```

**Session Snapshot 失效处理**：OpenClaw 在每个 session 中缓存一份 `skillsSnapshot`（含已发现的 skills 列表和格式化后的 prompt），存储在 NFS 上的 `sessions.json` 中。NFS 不支持 Linux inotify，导致 OpenClaw 的 chokidar file watcher 无法检测 skill 文件变化，已有 session 的 snapshot 永远不会自动刷新。

因此，基因安装/卸载后必须执行三步：

1. **`openclaw_session.invalidate_skill_snapshots(mount_path)`**：清除 `sessions.json` 中所有 session 的 `skillsSnapshot` 字段，使下次聊天时强制重建
2. **`openclaw_session.inject_evolution_notification(mount_path, gene_name, action)`**：向所有活跃 session 的 `.jsonl` 对话文件追加一对 user+assistant 消息（"你获取了新基因 X，完成了一轮进化"），防止 LLM 因对话历史中的旧 skill 列表而重复过时回答；同时将 `systemSent` 重置为 `false` 以强制下次对话重建 system prompt。注意：`systemSent` 定义在 OpenClaw 的 `SessionEntry` 类型中（`src/config/sessions/types.ts`），但读取逻辑在闭源的 `@mariozechner/pi-coding-agent` NPM 依赖中，OpenClaw 开源代码仅定义类型、不包含使用该字段的运行时逻辑。注意：`sessionFile` 字段存储的是容器内绝对路径（如 `/root/.openclaw/.../xxx.jsonl`），通过 NFS 操作时必须转换为本地挂载路径（`sessions_path.parent / Path(session_file).name`）
3. **`restart_instance()`**：重启 Pod（scale 0→N），使 OpenClaw 进程重新加载 skill 文件

**对话历史污染问题**：仅清除 snapshot 不够——NFS 上的 `.jsonl` 对话文件保留了旧 Pod 的回复（如 Agent 说"我只有 3 个技能"）。新 Pod 启动后 LLM 看到历史消息中自己的旧回答，会直接复述而不重新检查 system prompt。通过注入进化通知消息，LLM 的对话上下文中包含了明确的 skill 变更确认，避免重复过时信息。

### 5.2 ResourceBuilder — K8s 资源构建

不使用 Helm，通过 `kubernetes-asyncio` 直接构建和创建 K8s 资源对象：

```python
# app/services/resource_builder.py

def build_labels(instance_name, instance_id, image_tag) -> dict:
    """统一标签（managed-by: clawbuddy）"""

def build_configmap(name, namespace, env_vars, labels) -> V1ConfigMap:
    """构建环境变量 ConfigMap"""

def build_deployment(name, namespace, image, replicas, ...) -> V1Deployment:
    """构建 Deployment（含健康检查、资源配额、envFrom）"""

def build_service(name, namespace, labels, port=18789) -> V1Service:
    """构建 ClusterIP Service（端口 18789）"""

def build_ingress(name, namespace, host, labels, tls_secret_name=None, port=18789) -> V1Ingress:
    """构建 Ingress（nginx IngressClass + TLS + WebSocket annotations）"""

async def create_openclaw_instance(k8s, req, instance_id):
    """按顺序创建：ConfigMap → Deployment → Service → Ingress"""

async def update_openclaw_instance(k8s, req, instance_id):
    """更新 ConfigMap/Secret（含 OPENCLAW_FORCE_RECONFIG=true）→ 重启 Pod → 回写 FORCE_RECONFIG=false"""

async def delete_openclaw_instance(k8s, namespace):
    """删除整个命名空间（级联删除所有资源：Deployment/Service/Ingress/PVC/ConfigMap 等）"""

async def rollback_openclaw_instance(k8s, instance, target_record):
    """从 DB 历史记录回滚"""
```

### 5.3 K8sClient — K8s API 封装

```python
class K8sClientManager:
    """管理多集群的 K8s Client 实例，按 cluster_id 缓存"""

    _clients: dict[str, kubernetes.client.ApiClient] = {}

    def get_client(self, cluster_id: str) -> K8sClient:
        """获取或创建 K8s Client（解密 KubeConfig → 加载 → 缓存）"""

    def remove_client(self, cluster_id: str):
        """集群删除时清理"""

class K8sClient:
    """单集群操作封装"""

    # ---- Cluster ----
    async def get_version(self) -> str
    async def list_nodes(self) -> list[NodeInfo]
    async def get_cluster_resources(self) -> ClusterOverview

    # ---- Namespace ----
    async def list_namespaces(self) -> list[str]
    async def ensure_namespace(self, ns: str)

    # ---- Deployment ----
    async def get_deployment(self, ns: str, name: str) -> V1Deployment
    async def list_deployments(self, ns: str, label_selector: str) -> list
    async def restart_deployment(self, ns: str, name: str)
    async def scale_deployment(self, ns: str, name: str, replicas: int)

    # ---- Pod ----
    async def list_pods(self, ns: str, label_selector: str) -> list[PodInfo]
    async def get_pod_logs(self, ns: str, pod: str,
                           container: str, lines: int) -> str
    async def stream_pod_logs(self, ns: str, pod: str,
                              container: str) -> AsyncIterator[str]

    # ---- Service / Ingress ----
    async def get_service(self, ns: str, name: str) -> ServiceInfo
    async def get_ingress(self, ns: str, name: str) -> IngressInfo

    # ---- Events ----
    async def list_events(self, ns: str, field_selector: str) -> list[K8sEvent]

    # ---- Metrics (Metrics Server) ----
    async def get_pod_metrics(self, ns: str, pod: str) -> PodMetrics

    # ---- Watch ----
    async def watch_pods(self, ns: str, label_selector: str) -> AsyncIterator
    async def watch_events(self, ns: str) -> AsyncIterator
```

### 5.4 K8sWatcher — 实时事件监听

```
K8sWatcher (后台常驻任务)
│
├── watch_pods_loop(cluster_id, namespace, label_selector)
│   ├─ 使用 K8s Watch API 监听 Pod 变更
│   ├─ 检测状态变更 → broadcast("instance:{id}", {status, pods})
│   └─ 连接断开 → 指数退避重连
│
├── watch_events_loop(cluster_id, namespace)
│   ├─ 监听 K8s Events
│   ├─ 新事件 → broadcast("events:{namespace}", event)
│   └─ Warning 事件 → 额外触发告警逻辑
│
└── watch_metrics_loop(cluster_id)  (M3)
    ├─ 定时 (30s) 拉取 Metrics Server 数据
    └─ broadcast("metrics", {pods_metrics})
```

### 5.5 LlmConfigService — LLM 配置读写（NFS）

LLM 配置不走 ConfigMap / env vars，直接读写 PVC 上的 `/root/.openclaw/openclaw.json`。

**读写方式**：通过临时 NFS 挂载（`nfs_mount.py`），不依赖 Pod 运行状态。NFS 地址从 K8s PV spec 自动发现，零配置。

```
读写流程:
  1. 从 Instance 获取 PVC 名称 ({slug}-root-data) 和 namespace
  2. K8s API: read PVC → spec.volumeName → read PV → spec.nfs.server + spec.nfs.path
  3. mount -t nfs {server}:{path} /tmp/clawbuddy-nfs/{namespace}/
  4. 通过本地文件 I/O 读写 .openclaw/openclaw.json
  5. umount + rmdir（操作完立即卸载）

NFS 地址信息在内存中按 instance_id 缓存，避免重复查询 K8s API。
```

```
sync_openclaw_llm_config(instance):
  1. 查 user_llm_configs (instance.created_by + instance.org_id)
  2. 按 key_source 分支构造 models.providers JSON:
     - org → proxy baseUrl + wp_api_key（走 LLM 代理）
     - personal → 查 UserLlmKey 取真实 API Key + Provider 默认 base URL（直连）
  3. 如果 user_llm_config.selected_models 非空，写入 models 数组；自定义 Provider（minimax-*）同时写入 api 字段
  4. NFS 挂载 → 读取 openclaw.json → 合并 models.providers → 写回 → 卸载

restart_openclaw(instance):
  1. 调用 sync_openclaw_llm_config（NFS 写入）
  2. kubectl exec kill -SIGTERM 1 优雅重启 OpenClaw（仅此处保留 kubectl exec）
  3. 轮询 Pod Ready（或超时）
```

初次部署和后续更新用同一个函数。新增/删除 Provider 需要重启 OpenClaw；切换同一 Provider 的 Key 来源也需要重启（因为 base URL 和 key 不同）。

**系统要求**：mount 需要 root 或 CAP_SYS_ADMIN 权限。本地开发需 sudo 运行后端，VKE 部署需容器 securityContext 配置。

### 5.6 LLM 代理转发

LLM 代理仅服务于组织 Key 模式。个人 Key 模式下 OpenClaw 直连 Provider，不经过此代理。

```
LlmProxy 请求处理流程:
  1. Authorization: Bearer <wp_api_key> → 查 instances.wp_api_key（索引），未命中时回退查 proxy_token
  2. URL path 提取 provider → 查 user_llm_configs
  3. 按 (org_id, provider) 自动查找首条 active 的 org_llm_key → 额度检查
  4. 按 Provider 设置认证头 (bearer / x-api-key / query param)
  5. 注入 stream_options 确保流式响应含 usage
  6. httpx.AsyncClient 流式转发
  7. 解析响应 usage → INSERT llm_usage_logs
```

---

## 六、SSE 实时推送设计

> 选择 SSE 而非 WebSocket 的原因：ClawBuddy 的实时场景全部是服务端单向推送（日志、事件、进度、状态），客户端命令（订阅/暂停）用普通 REST 请求即可。SSE 更简单、HTTP 原生、自动重连、负载均衡零配置。

### 6.1 SSE 流端点

| 端点 | 用途 | 认证 | 参数 |
|------|------|------|------|
| `GET /stream/instances/:id/status` | 实例状态 + Pod 变更 | Bearer JWT | — |
| `GET /stream/instances/:id/events` | K8s 事件流 | Bearer JWT | `?type=Warning` |
| `GET /stream/instances/:id/logs` | Pod 实时日志 | Bearer JWT | `?pod=xx&container=xx&tail=100` |
| `GET /stream/deploy/:deploy_id/progress` | 部署进度 | Bearer JWT | — |
| `GET /stream/notifications` | 全局通知（集群状态、告警） | Bearer JWT | — |

认证走标准 HTTP `Authorization: Bearer <jwt>`，和 REST API 完全一致。

### 6.2 SSE 消息格式

标准 SSE 格式：`event:` + `data:` + `id:`（用于断点续传）

**实例状态流** `GET /stream/instances/:id/status`

```
event: instance_status
id: evt-1707554321-001
data: {"instance_id":"xxx","status":"running","replicas":1,"available":1,"pods":[{"name":"...","status":"Running","restart_count":0}]}

event: pod_changed
id: evt-1707554322-002
data: {"pod":"xxx-m4p8q","status":"Running","phase":"Running","restart_count":0}
```

**K8s 事件流** `GET /stream/instances/:id/events`

```
event: k8s_event
id: evt-1707554345-003
data: {"namespace":"oc-prod-1","type":"Warning","reason":"BackOff","object":"pod/xxx-abc12","message":"Back-off restarting failed container"}
```

**日志流** `GET /stream/instances/:id/logs?pod=xx&container=openclaw&tail=100`

```
event: log
id: log-1707554321123
data: {"ts":"2026-02-10T10:32:01.123Z","line":"INFO  Server started on port 18789","level":"INFO"}

event: log_end
data: {"reason":"pod_terminated"}
```

**部署进度** `GET /stream/deploy/:deploy_id/progress`

```
event: progress
id: deploy-step-3
data: {"deploy_id":"xxx","status":"in_progress","progress":45,"current_step":"waiting_pods","steps":[{"name":"create_namespace","status":"done"},{"name":"create_resources","status":"done"},{"name":"waiting_pods","status":"running","message":"1/1 Ready"},{"name":"service_ready","status":"pending"}]}

event: complete
data: {"deploy_id":"xxx","status":"success","elapsed_ms":12500}
```

**全局通知** `GET /stream/notifications`

```
event: notification
data: {"level":"warning","title":"Pod 异常","message":"openclaw-prod-1 进入 CrashLoopBackOff"}

event: cluster_status
data: {"cluster_id":"xxx","status":"disconnected"}
```

### 6.3 前端使用

```typescript
// 浏览器 EventSource — 内置自动重连 + Last-Event-ID 断点续传
const es = new EventSource('/api/v1/stream/instances/xxx/logs?pod=xx&container=openclaw&tail=100', {
  headers: { 'Authorization': `Bearer ${token}` }  // 需要 polyfill 或 fetch-based SSE
});
es.addEventListener('log', (e) => appendLog(JSON.parse(e.data)));
es.addEventListener('log_end', () => es.close());

// 关闭页面 → 连接自动断开 → 后端 generator 清理
```

> 注意：原生 `EventSource` 不支持自定义 Header。实例级 SSE 流用 `@microsoft/fetch-event-source` 或类似库支持 Bearer Token。工作区 events 端点改用原生 `EventSource` + URL query parameter 传递短时效 SSE token（`POST /workspaces/sse-token` 签发，5 分钟有效）。

### 6.4 后端实现要点

- 用 FastAPI `StreamingResponse(generator(), media_type="text/event-stream")`
- 实例级 SSE 流走标准 `Depends(get_current_user)` 认证（Authorization 请求头）
- 工作区 events 端点走 `Depends(get_current_user_from_query)` 从 URL `?token=` 读 JWT（因 `EventSource` 不支持自定义请求头）
- 每个 SSE 连接对应一个 `async generator`，内部调 K8s Watch/日志流
- 客户端断开 → generator 被 cancel → 自动清理 K8s Watch
- 心跳：每 30 秒发 `:keepalive\n\n`（SSE 注释行），防止代理超时
- HTTP/2 下多个 SSE 连接复用同一 TCP，不浪费

---

## 七、认证与安全

### 7.1 认证流程

```
    前端                       后端                        飞书
     │                          │                          │
     │  1. 打开登录页            │                          │
     │  2. 跳转飞书 OAuth ──────────────────────────────▶ │
     │                          │                          │
     │  3. 用户授权，带 code 跳回│                          │
     │ ◀────────────────────────┤                          │
     │                          │                          │
     │  4. POST /auth/feishu/callback {code} ──────▶│     │
     │                          │  5. code 换 access_token──▶│
     │                          │  ◀── user_info ──────────│
     │                          │                          │
     │                          │  6. 查找/创建 User 记录  │
     │                          │  7. 签发 JWT             │
     │  ◀─── {access_token, refresh_token, user} ──│      │
     │                          │                          │
     │  8. 后续请求带 Authorization: Bearer <jwt>          │
     │  ─────────────────────▶│                            │
     │                          │  9. 校验 JWT             │
     │  ◀─── 业务响应 ─────────│                            │
```

### 7.2 JWT 策略

| 项 | 值 |
|------|------|
| 算法 | HS256 |
| access_token 有效期 | 2 小时 |
| refresh_token 有效期 | 7 天 |
| Payload | `{ sub: user_id, role: admin\|user, exp, iat }` |

### 7.3 KubeConfig 加密存储

```python
# AES-256-GCM 加密
def encrypt_kubeconfig(plain: str, key: bytes) -> str:
    """加密 KubeConfig 明文 → Base64 密文存入数据库"""

def decrypt_kubeconfig(cipher: str, key: bytes) -> str:
    """从数据库读取密文 → 解密为 KubeConfig 明文"""

# 密钥来源: 环境变量 CLAWBUDDY_ENCRYPTION_KEY
# 或 K8s Secret 挂载
```

### 7.4 API 权限矩阵

| 资源 | 操作 | admin | user |
|------|------|-------|------|
| Cluster | 添加/编辑/删除 | ✅ | ❌ |
| Cluster | 查看 | ✅ | ✅ |
| Instance | 创建 | ✅ | ✅ |
| Instance | 编辑/删除 | ✅ | 仅自己创建的 |
| Instance | 查看 | ✅ | ✅ |
| Deploy | 创建/取消 | ✅ | ✅ (自己的实例) |
| Logs | 查看 | ✅ | ✅ |
| Monitor | 查看 | ✅ | ✅ |
| Settings | 系统设置 | ✅ | ❌ |
| Audit | 查看 | ✅ | ❌ |

---

## 八、错误处理策略

### 8.1 异常层次

```python
class ClawBuddyError(Exception):
    """基础异常"""
    code: int = 50001
    message: str = "内部错误"

class AuthError(ClawBuddyError):
    """认证异常 10xxx"""

class ClusterError(ClawBuddyError):
    """集群异常 20xxx"""

class InstanceError(ClawBuddyError):
    """实例异常 30xxx"""

class DeployError(ClawBuddyError):
    """部署异常 40xxx"""
```

### 8.2 全局异常处理器

```python
@app.exception_handler(ClawBuddyError)
async def clawbuddy_error_handler(request, exc):
    return JSONResponse(
        status_code=200,     # 业务错误统一 200，通过 code 区分
        content={"code": exc.code, "message": exc.message, "data": None}
    )

@app.exception_handler(Exception)
async def generic_error_handler(request, exc):
    logger.exception("Unhandled error")
    return JSONResponse(
        status_code=500,
        content={"code": 50001, "message": "服务器内部错误", "data": None}
    )
```

### 8.3 K8s API 错误处理

```python
async def safe_k8s_call(func, *args, **kwargs):
    """包装 K8s API 调用，统一处理异常"""
    try:
        return await func(*args, **kwargs)
    except ApiException as e:
        if e.status == 401:
            raise ClusterError(20003, "集群认证失败，请检查 KubeConfig")
        elif e.status == 403:
            raise ClusterError(20004, "权限不足，请检查 RBAC 配置")
        elif e.status == 404:
            raise InstanceError(30002, "资源不存在")
        else:
            raise ClusterError(20099, f"K8s API 错误: {e.reason}")
    except MaxRetryError:
        raise ClusterError(20001, "集群连接失败，请检查网络")
```

---

## 九、配置管理

```python
# core/config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # === 应用 ===
    app_name: str = "ClawBuddy"
    app_version: str = "0.1.0"
    debug: bool = False
    host: str = "0.0.0.0"
    port: int = 8000

    # === 数据库（火山云 RDS PostgreSQL） ===
    database_url: str  # postgresql+asyncpg://user:pass@host:5432/clawbuddy

    # === JWT ===
    jwt_secret: str
    jwt_algorithm: str = "HS256"
    jwt_access_expire_minutes: int = 120
    jwt_refresh_expire_days: int = 7

    # === 加密 ===
    encryption_key: str          # AES-256 密钥 (32 bytes hex)

    # === 飞书 SSO ===
    feishu_app_id: str
    feishu_app_secret: str
    feishu_redirect_uri: str

    # === 日志 ===
    log_level: str = "INFO"

    class Config:
        env_file = ".env"
        env_prefix = "CLAWBUDDY_"

### 日志系统

后端使用 Python 标准库 `logging` + `RotatingFileHandler` 实现本地滚动日志：

| 配置项 | 值 |
|--------|-----|
| 日志文件 | `logs/clawbuddy.log` |
| 单文件大小 | 10MB |
| 保留历史文件数 | 5（加当前文件共约 60MB） |
| 日志格式 | `时间 级别 [模块名] 消息` |
| 输出目标 | 同时输出到文件和控制台（stdout） |
| 日志级别 | INFO（通过 root logger 配置） |

日志在 `main.py` 模块级别初始化，应用启动即生效。`logs/` 目录已在 `.gitignore` 中排除。
```

**环境变量示例 (.env)**:

```bash
CLAWBUDDY_JWT_SECRET=your-secret-key-here
CLAWBUDDY_ENCRYPTION_KEY=your-32-byte-hex-key
CLAWBUDDY_FEISHU_APP_ID=cli_xxxx
CLAWBUDDY_FEISHU_APP_SECRET=xxxx
CLAWBUDDY_FEISHU_REDIRECT_URI=https://clawbuddy.example.com/api/v1/auth/feishu/callback
CLAWBUDDY_DATABASE_URL=postgresql+asyncpg://clawbuddy:<password>@<rds-host>:5432/clawbuddy
```

---

## 十、启动流程与生命周期

```python
# main.py
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期管理"""
    # === 启动 ===
    logger.info("ClawBuddy starting...")

    # 1. 初始化数据库连接
    await init_db()

    # 2. 初始化 K8s Client Manager
    k8s_manager = K8sClientManager()
    await k8s_manager.init_from_db()     # 从 DB 加载所有集群的 KubeConfig

    # 3. 初始化 SSE EventBus
    broadcaster = EventBroadcaster()

    # 4. 启动 K8s Watcher（后台常驻）
    watcher = K8sWatcher(k8s_manager, broadcaster)
    watcher_task = asyncio.create_task(watcher.start())

    # 5. 启动集群健康巡检（每 60s 检测连接 + Token 过期）
    health_checker = ClusterHealthChecker(k8s_manager, broadcaster, db_factory)
    await health_checker.start()

    yield {
        "k8s_manager": k8s_manager,
        "broadcaster": broadcaster,
    }

    # === 关闭 ===
    logger.info("ClawBuddy shutting down...")
    await health_checker.stop()
    watcher_task.cancel()
    await k8s_manager.close_all()
    await close_db()

app = FastAPI(
    title="ClawBuddy API",
    version=settings.app_version,
    lifespan=lifespan,
)

# 中间件
app.add_middleware(CORSMiddleware, ...)
app.add_middleware(AuditMiddleware)       # 操作审计

# 挂载路由
app.include_router(auth_router,      prefix="/api/v1/auth",      tags=["认证"])
app.include_router(cluster_router,   prefix="/api/v1/clusters",  tags=["集群"])
app.include_router(instance_router,  prefix="/api/v1/instances", tags=["实例"])
app.include_router(deploy_router,    prefix="/api/v1/deploy",    tags=["部署"])
app.include_router(log_router,       prefix="/api/v1/logs",      tags=["日志"])
app.include_router(monitor_router,   prefix="/api/v1/monitor",   tags=["监控"])
app.include_router(stream_router,    prefix="/stream",           tags=["SSE 实时推送"])

# 静态文件 (前端)
app.mount("/", StaticFiles(directory="static", html=True), name="frontend")
```

---

## 十一、依赖清单

```
# requirements.txt

# === Web 框架 ===
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*

# === 数据库 ===
sqlalchemy[asyncio]==2.0.*
asyncpg==0.30.*                    # PostgreSQL 异步驱动（火山云 RDS）

# === K8s ===
kubernetes-asyncio==34.*

# === 认证 & 安全 ===
python-jose[cryptography]==3.3.*   # JWT
cryptography==44.*                 # AES 加密
httpx==0.28.*                      # 飞书 API 调用

# === 工具 ===
pydantic-settings==2.7.*           # 配置管理
pyyaml==6.*                        # YAML 解析（KubeConfig）

# === 测试 ===
pytest==8.*
pytest-asyncio==0.25.*
httpx                              # TestClient
```

---

## 十二、部署架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        K8s Cluster                                │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Namespace: clawbuddy                                    │    │
│  │                                                         │    │
│  │  ┌─────────────────────────────────────────────────┐   │    │
│  │  │  Deployment: clawbuddy                           │   │    │
│  │  │  replicas: 2                                     │   │    │
│  │  │                                                  │   │    │
│  │  │  ┌──────────────────────────────────────────┐   │   │    │
│  │  │  │  Pod                                     │   │   │    │
│  │  │  │                                          │   │   │    │
│  │  │  │  Container: clawbuddy                    │   │   │    │
│  │  │  │  image: clawbuddy:v0.1.0                 │   │   │    │
│  │  │  │  ports: 8000                             │   │   │    │
│  │  │  │                                          │   │   │    │
│  │  │  │  前端静态文件 ─── /static/                │   │   │    │
│  │  │  │  后端 API ────── /api/v1/                │   │   │    │
│  │  │  │  SSE 推送 ────── /stream/                 │   │   │    │
│  │  │  │                                          │   │   │    │
│  │  │  │  # 数据库为火山云 RDS PostgreSQL（外部）    │   │   │    │
│  │  │  │  # 无需本地 PVC 存储数据库文件              │   │   │    │
│  │  │  │                                          │   │   │    │
│  │  │  │  envFrom:                                │   │   │    │
│  │  │  │    - secretRef: clawbuddy-secret          │   │   │    │
│  │  │  │    - configMapRef: clawbuddy-config       │   │   │    │
│  │  │  └──────────────────────────────────────────┘   │   │    │
│  │  └─────────────────────────────────────────────────┘   │    │
│  │                                                         │    │
│  │  ┌──────────────────┐  ┌─────────────────────────┐    │    │
│  │  │ Service           │  │ Ingress                  │    │    │
│  │  │ type: ClusterIP   │  │ host: clawbuddy.xxx.com  │    │    │
│  │  │ port: 8000        │  │ path: /                  │    │    │
│  │  └──────────────────┘  │ → Service:8000            │    │    │
│  │                         └─────────────────────────┘    │    │
│  │                                                         │    │
│  │  ┌──────────────────┐  ┌─────────────────────────┐    │    │
│  │  │ Secret            │  │ ConfigMap                │    │    │
│  │  │ clawbuddy-secret  │  │ clawbuddy-config         │    │    │
│  │  │                  │  │                          │    │    │
│  │  │ JWT_SECRET       │  │ LOG_LEVEL=INFO           │    │    │
│  │  │ ENCRYPTION_KEY   │  │ FEISHU_REDIRECT_URI=...  │    │    │
│  │  │ FEISHU_APP_SECRET│  │                          │    │    │
│  │  └──────────────────┘  └─────────────────────────┘    │    │
│  │                                                         │    │
│  │  # PVC 不再需要 — 数据库使用火山云 RDS PostgreSQL       │    │
│  │                                                         │    │
│  │  ┌──────────────────┐                                  │    │
│  │  │ ServiceAccount    │                                  │    │
│  │  │ clawbuddy-sa      │                                  │    │
│  │  │ + ClusterRole     │  ← 操作其他 NS 的资源权限        │    │
│  │  │   Binding         │                                  │    │
│  │  └──────────────────┘                                  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Namespace: production / testing / ...                    │    │
│  │                                                         │    │
│  │  (由 ClawBuddy 管理的 OpenClaw 实例)                     │    │
│  │  Deployment + Service + Ingress per instance             │    │
│  └─────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

### Dockerfile (多阶段构建)

```dockerfile
# Stage 1: 前端构建
FROM node:20-alpine AS frontend
WORKDIR /app/frontend
COPY claw-buddy-frontend/package*.json ./
RUN npm ci
COPY claw-buddy-frontend/ ./
RUN npm run build

# Stage 2: 后端运行
FROM python:3.12-slim AS backend
WORKDIR /app

COPY claw-buddy-backend/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY claw-buddy-backend/ ./
COPY --from=frontend /app/frontend/dist ./static

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### docker-compose.yml (本地开发)

```yaml
version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: claw-buddy-backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./claw-buddy-backend:/app    # 代码热更新
      - clawbuddy-data:/app/data
    env_file:
      - .env
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

  frontend:
    build:
      context: ./claw-buddy-frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./claw-buddy-frontend/src:/app/src
    environment:
      VITE_API_BASE_URL: http://localhost:8000

volumes:
  clawbuddy-data:
```

---

## 十三、持久化存储架构

### 13.1 架构决策

OpenClaw 实例需要持久化存储来保存运行数据（配置、聊天记录等），挂载到容器的 `/root` 目录。

**决策**: 使用火山云 NAS 极速型 + NFS CSI 动态供给（subpath 模式）。

**原因**:
- 火山云 EBS 云盘仅支持 `ReadWriteOnce`，不适合多副本或迁移场景
- NAS NFS 支持 `ReadWriteMany`，一块 NAS 可通过 subpath 模式被多个 PVC 共享
- 动态供给每个 PVC 自动创建子目录，无需手动创建 PV
- `archiveOnDelete: true` 在 PVC 删除时仅归档子目录（重命名），不物理删除数据
- `reclaimPolicy: Retain` 额外保障数据安全

### 13.2 StorageClass 管理

- 集群管理员需要手动 `kubectl apply` 创建 StorageClass（一次性操作）
- YAML 模板: `claw-buddy-artifacts/k8s/nas-storageclass.yaml`
- 前提: VKE 集群需安装 `csi-nas` 组件
- 未来添加新存储类型（如容量型 NAS）也是同样流程：创建新的 StorageClass

### 13.3 实例级存储选择

每个实例独立记录使用的 StorageClass（`instances.storage_class` 字段），部署时用户可在前端选择：

```
GET /api/v1/storage-classes
→ 从 K8s 集群动态读取可用 StorageClass 列表
→ 前端部署页在「资源配额」步骤展示下拉选择器
→ 选择结果随 DeployRequest 一起提交到后端
→ 后端 deploy_service.py 使用该值创建 PVC
```

### 13.4 PVC 访问模式

所有 PVC 统一使用 `ReadWriteMany` 访问模式（NFS 天然支持）。

### 13.5 未来迁移方案

数据模型已支持实例级存储切换，未来实现跨存储迁移时：

1. 创建新 PVC（目标 StorageClass）
2. 启动 Job 将旧 PVC 数据拷贝到新 PVC
3. 更新 Instance 的 `storage_class` 和 Deployment 的 volume 引用
4. 删除旧 PVC

---

## 十四、模块与里程碑对照

| 模块 | M0 | M1 | M2 | M3 |
|------|:--:|:--:|:--:|:--:|
| main.py + 启动流程 | ✅ | | | |
| core/ (config, security, deps) | ✅ | | | |
| auth (飞书 SSO + JWT) | | ✅ | | |
| clusters (CRUD + 连接测试) | | ✅ | | |
| clusters (多集群管理) | | | | ✅ |
| instances (列表 + 详情) | | ✅ | | |
| instances (重启/扩缩容/修改配置) | | | ✅ | |
| deploy (预检 + 部署 + 进度) | | ✅ | | |
| deploy (滚动更新) | | | ✅ | |
| logs (REST 历史日志) | | | ✅ | |
| SSE (日志流) | | | ✅ | |
| SSE (部署进度) | | ✅ | | |
| SSE (事件流 + 状态推送) | | | | ✅ |
| monitor (Pod/Events/History) | | | | ✅ |
| monitor (Metrics 曲线) | | | | ✅ |
| monitor (版本回滚) | | | | ✅ |
| K8sWatcher (后台监听) | | ✅ (基础) | ✅ (完善) | |
| ResourceBuilder (资源构建器) | | ✅ | ✅ | |
| AuditLog | | | | ✅ |
| 权限控制 (admin/user) | | | | ✅ |
| 飞书告警推送 | | | | ✅ |
| LLM Key 管理 (数据模型 + API) | | | ✅ | |
| LLM 代理转发 (proxy + token 计数) | | | ✅ | |
| LLM 配置读写 OpenClaw (NFS 临时挂载) | | | ✅ | |
| ~~SQLite → PostgreSQL 迁移~~ | ✅ | | | | 已从 M1 起直接使用火山云 RDS PostgreSQL |
| 工作区群聊广播 + Channel Plugin | | | | ✅ |
| SSE + Agent 协同（长连接） | | | | ✅ |

---

## 十五、工作区群聊协同

### 核心概念

工作区 = 群聊室。用户消息广播给所有 Agent，各 Agent 根据自身设定决定是否回复。每个 Agent 是独立的 OpenClaw 实例，session 存储物理隔离，ClawBuddy 负责消息同步。

### 通信架构（SSE 长连接）

原先的 HTTP Webhook 方式（`POST /webhook/clawbuddy`）已废弃，改为 **SSE 长连接**：

- **Channel Plugin** 在实例内启动 SSE 服务（端口 9721，Node.js 内置 `http` 模块，零依赖）
- **后端** 主动连接各实例的 SSE 端点：`https://<ingress_domain>/sse/events`
- **心跳**：Plugin 每 15 秒发送 heartbeat，后端 45 秒内无事件则视为连接死亡
- **无监听检测**：Plugin 的 `sendText()` 在 `clients.size === 0` 时抛错，提示无后端连接

### 连接生命周期

| 时机 | 动作 |
|------|------|
| Agent 加入工作区（含重启后） | 后端建立到该实例的 SSE 连接 |
| Agent 离开工作区 | 后端断开对应 SSE 连接 |
| 后端启动 | 恢复所有运行中实例的 SSE 连接 |

### 数据模型

```python
class WorkspaceMessage(BaseModel):
    workspace_id: str       # FK -> workspaces.id
    sender_type: str        # "user" | "agent" | "system"
    sender_id: str          # user_id / instance_id / "system"
    sender_name: str
    content: str
    message_type: str       # "chat" | "collaboration" | "system"
    target_instance_id: str | None
    depth: int              # 防循环深度计数

class WorkspaceChatRequest(BaseModel):
    message: str
    mentions: list[str] | None = None   # @ 提及的 Agent instance_id 列表
```

### API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/workspaces/{ws}/chat` | 群聊广播：用户消息发给所有 Agent。请求体 `{ message, mentions? }` |
| GET | `/workspaces/{ws}/messages` | 获取工作区消息历史 |
| POST | `/workspaces/{ws}/system-message` | 持久化系统消息（斜杠命令结果等），不触发 Agent 回复 |
| GET | `/workspaces/{ws}/events?token=xxx` | SSE 事件流（含 agent:typing/chunk/done/error/collaboration/status、system:welcome/info） |
| POST | `/workspaces/sse-token` | 签发 5 分钟短时效 SSE token |

**events 端点认证**：浏览器 `EventSource` 不支持自定义 HTTP 请求头，因此 events 端点通过 URL query parameter `?token=` 传递 JWT。前端先调 `POST /workspaces/sse-token` 获取短时效 token，降级时使用 access token。后端通过 `get_current_user_from_query` 依赖从 query 读取并验证。

> 不再提供 `POST /webhook/clawbuddy`。Agent 消息通过 SSE 长连接由后端主动拉取。

### 消息流

1. 用户发送消息 -> 记录到 `workspace_messages` -> 并发调用所有运行中 Agent
2. 如请求携带 `mentions`（@ 提及 instance_id 列表），被 @ 的 Agent system prompt 追加"必须回复"提示；未被 @ 的 Agent 追加"可 NO_REPLY"提示。无 mentions 时保持原有全广播行为
3. 每个 Agent 通过独立 `asyncio.Task` 流式响应
4. 前 20 字符缓冲检测 `NO_REPLY`（OpenClaw 内置静默标记）-> 丢弃内容，但仍广播 `agent:done`（不含 `full_content`）以清除前端 typing 指示器
5. Agent 主动协同：`send -t clawbuddy -to "agent:xxx"` -> Plugin 通过 SSE 广播 -> 后端 `sse_listener` 接收 -> `collaboration_service` 路由到目标 Agent

### Agent 状态实时推送

实例状态变化（如重启完成 `restarting -> running`）由 `_monitor_restart` 异步任务监控，完成后通过 `agent:status` SSE 事件广播到工作区前端，前端实时更新 agent 列表中的状态徽标，无需手动刷新。

### SSE 连接健康状态

`SSEListenerManager` 通过 `_healthy` 集合精确跟踪每个实例的实际 SSE 连接状态（区别于 `connected_instances` 仅表示 task 未结束）：

- **连接成功**（`_listen_once` 收到 200 响应）-> `_healthy.add(instance_id)` + 广播 `agent:sse_connected` 事件
- **连接断开**（异常/超时/取消）-> `_healthy.discard(instance_id)` + 广播 `agent:sse_disconnected` 事件
- **实例删除** -> `delete_instance()` 中主动调用 `sse_listener_manager.disconnect()` 断开连接，避免幽灵连接无限重试

**启动恢复**：lifespan 中查询 `status IN ('running', 'restarting')` 的工作区实例，对卡在 `restarting` 的实例自动恢复为 `running`（防止后端重启导致 `_monitor_restart` 异步任务丢失而状态卡死），然后为所有实例建立 SSE 连接。

`AgentBrief` schema 包含 `sse_connected: bool` 字段，由 `_agent_brief()` 查询 `sse_listener_manager.healthy_instances` 填充。前端工作区画布根据此字段展示连接状态视觉差异（已连接: 正常颜色; 断开: 灰色/虚线边框/半透明）。

### 系统消息持久化

斜杠命令（`/status`、`/restart`、`/remove` 等）的结果通过 `POST /workspaces/{ws}/system-message` 持久化到 `workspace_messages` 表（`sender_type="system"`），并通过 `system:info` SSE 事件广播。刷新页面后仍可在聊天历史中看到。`/clear` 例外，仅清空前端显示。

### Agent 入群欢迎消息

Agent 加入工作区后自动触发自我介绍：

1. `add_agent()` 完成 channel plugin 部署和 SSE 连接后，调度 `_send_welcome_message()` 异步任务
2. 等待 20 秒（15s SSE 延迟 + 5s 缓冲，确保实例完全就绪）
3. 记录一条 `sender_type="system"` 的消息到 DB，广播 `system:welcome` SSE 事件到前端
4. 调用 `collaboration_service._invoke_target_agent()` 向 Agent 发送自我介绍提示
5. Agent 的回复通过 `agent:typing` -> `agent:chunk` -> `agent:done` 事件链自动出现在群聊中
6. 前端系统消息以居中灰色气泡展示（如"AgentXXX 已加入工作区"）

### Session 机制

- Header: `X-OpenClaw-Session-Key: workspace:{workspace_id}`
- 上下文注入通过 `system` role（成为 `extraSystemPrompt`，不存入 session）
- 只注入**其他成员**的近期消息，避免与 Agent 自身 session 重复

### Channel Plugin

独立包 `openclaw-channel-clawbuddy`，零运行时依赖，通过 NFS 分发 `.ts` 源文件到各实例，jiti 直接加载。

- **SSE 服务**：`src/sse-server.ts`，在 `index.ts` 注册时启动，监听 9721
- **广播**：`src/channel.ts` 的 `sendText()` 向所有已连接的后端客户端推送事件

### K8s 资源配置

| 资源 | 配置 |
|------|------|
| Container | 暴露端口 9721 |
| Service | 映射 9721 |
| Ingress | 路由 `/sse/` 到端口 9721，`proxy-buffering: off`，`proxy-read-timeout: 86400` |

### 关键文件

| 层级 | 文件 | 职责 |
|------|------|------|
| Plugin | `openclaw-channel-clawbuddy/src/sse-server.ts` | SSE 服务 |
| Plugin | `openclaw-channel-clawbuddy/src/channel.ts` | 广播、sendText |
| Plugin | `openclaw-channel-clawbuddy/index.ts` | 注册时启动 SSE |
| Backend | `app/services/sse_listener.py` | 连接管理、心跳检测、健康状态跟踪 |
| Backend | `app/services/collaboration_service.py` | 消息处理、路由 |
| Backend | `app/services/workspace_service.py` | 生命周期集成 |
| Backend | `app/main.py` | 启动时恢复所有连接 |
| K8s | `app/services/k8s/resource_builder.py` | 端口、Ingress 路径 |

### 防循环

- 用户消息 -> Agent 响应：只入库，不触发其他 Agent
- Agent send -> 目标 Agent：携带 `depth` 计数器，超过 `MAX_DEPTH=3` 停止

---

## 基因进化生态系统

### 数据模型

#### Gene 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| name | str(128) | 显示名称 |
| slug | str(128) unique | 唯一标识 |
| description | text | 详细描述（Markdown） |
| short_description | str(256) | 卡片摘要 |
| category | str(32) | 领域分类（开发/数据/运维/网络/创意/沟通/安全/效率） |
| tags | JSON | 标签数组（能力/性格/知识/工具） |
| source | enum | `official` / `clawhub` / `community` / `agent` |
| source_ref | str | 外部来源引用 |
| icon | str(32) | Lucide 图标名 |
| version | str(16) | 版本号 |
| manifest | JSON | 安装清单（skill + openclaw_config + learning） |
| dependencies | JSON | 依赖的其他基因 slug |
| synergies | JSON | 协同增效的基因 slug 列表 |
| parent_gene_id | FK Gene nullable | 变体溯源（null = 原创基因） |
| created_by_instance_id | FK Instance nullable | Agent 创造/变体来源实例 |
| install_count | int | 安装次数 |
| avg_rating | float | 用户平均评分 |
| effectiveness_score | float | 综合效能分 |
| is_featured | bool | 推荐 |
| review_status | enum nullable | `pending_owner` / `pending_admin` / `approved` / `rejected` |
| is_published | bool | 上架 |
| created_by | FK User nullable | 人类创建者 |
| org_id | FK Org nullable | 组织可见 |

#### Genome 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| name | str(128) | 显示名称 |
| slug | str(128) unique | 唯一标识 |
| description | text | 详细描述 |
| short_description | str(256) | 卡片摘要 |
| icon | str(32) | 图标 |
| gene_slugs | JSON | 包含的基因 slug 数组 |
| config_override | JSON | 额外配置覆盖 |
| install_count | int | 应用次数 |
| avg_rating | float | 评分 |
| is_featured | bool | 推荐 |
| is_published | bool | 上架 |
| created_by | FK User | 创建者 |
| org_id | FK Org nullable | |

#### InstanceGene 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| instance_id | FK Instance | |
| gene_id | FK Gene | |
| genome_id | FK Genome nullable | 通过哪个基因组安装的 |
| status | enum | `installing` / `learning` / `installed` / `learn_failed` / `failed` / `uninstalling` |
| installed_version | str(16) | 安装时的版本 |
| learning_output | text nullable | Agent 深度学习的个性化 SKILL.md |
| config_snapshot | JSON | 实际写入的配置快照 |
| agent_self_eval | float nullable | Agent 自评分（0-1） |
| usage_count | int | 该基因被实际使用的次数 |
| variant_published | bool | 是否已发布为市场变体 |
| installed_at | datetime | |

#### GeneEffectLog 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| instance_id | FK Instance | |
| gene_id | FK Gene | |
| metric_type | enum | `user_positive` / `user_negative` / `task_success` / `agent_self_eval` |
| value | float | 指标值 |
| context | text nullable | 上下文说明 |

#### GeneRating / GenomeRating 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| gene_id / genome_id | FK | |
| user_id | FK User | |
| rating | int | 1-5 |
| comment | text | |

### API 路由

#### 基因市场

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/genes` | 列表/搜索 |
| GET | `/genes/tags` | 标签统计 |
| GET | `/genes/featured` | 推荐基因 |
| GET | `/genes/:id` | 详情（含变体列表 + 协同推荐） |
| GET | `/genes/:id/variants` | 该基因的所有变体 |
| GET | `/genes/:id/synergies` | 协同基因列表 |
| GET | `/genes/:id/genomes` | 包含该基因的基因组列表 |
| POST | `/genes/:id/rate` | 评分 |

#### 基因组市场

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/genomes` | 列表/搜索 |
| GET | `/genomes/featured` | 推荐 |
| GET | `/genomes/:id` | 详情 |
| POST | `/genomes/:id/rate` | 评分 |

#### 实例基因管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/instances/:id/genes` | 已安装列表 |
| POST | `/instances/:id/genes/install` | 安装基因 |
| POST | `/instances/:id/genes/uninstall` | 卸载 |
| POST | `/instances/:id/genomes/apply` | 应用基因组 |
| POST | `/instances/:id/genes/:gene_id/publish-variant` | 发布学习产物为变体 |
| POST | `/instances/:id/genes/:gene_id/effectiveness` | 记录效能信号 |
| POST | `/instances/:id/genes/create` | 触发 Agent 创造新基因 |

#### 学习回调

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/genes/learning-callback` | Learning Plugin 学习结果回调 |
| POST | `/genes/creation-callback` | Learning Plugin 创造结果回调 |

#### Admin

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/admin/genes/stats` | 全局统计 |
| GET | `/admin/genes/activity` | 活动流 |
| GET | `/admin/genes/matrix` | 实例-基因矩阵 |
| GET | `/admin/genes/co-install` | 共装分析 |
| PUT | `/admin/genes/:id/review` | 审核 Agent 创造的基因 |

### SSE 事件

| SSE 事件 | 触发时机 | Payload |
|----------|----------|---------|
| `gene:install_start` | 直接安装开始 | instance_id, gene_slug |
| `gene:learn_start` | 学习任务已发送 | instance_id, gene_slug |
| `gene:learn_decided` | Agent 做出决策 | instance_id, gene_slug, decision, reason |
| `gene:installed` | 安装/学习完成 | instance_id, gene_slug, method |
| `gene:learn_failed` | 学习失败 | instance_id, gene_slug, reason |
| `gene:variant_published` | 变体发布到市场 | instance_id, gene_slug, variant_slug |
| `gene:created` | Agent 创造了新基因 | instance_id, gene_slug, gene_name |
| `gene:effect_logged` | 效能信号记录 | instance_id, gene_slug, metric_type |
| `gene:recommended` | Agent 在群聊中推荐基因 | workspace_id, from_instance, to_instance, gene_slug |

### Learning Channel Plugin

独立插件 `openclaw-channel-learning`，与 clawbuddy plugin 同构部署。支持学习模式和创造模式两种任务。

### 安装/学习引擎

1. 用户请求安装基因
2. 后端去重检查 + 创建 InstanceGene
3. 检查 Agent 是否有 meta-learning 基因
4. 有学习能力：status=learning -> POST 学习任务到 Learning Plugin -> Agent 评估决策 -> callback 回传结果
5. 无学习能力：status=installing -> 直接写入 SKILL.md
6. 合并 openclaw.json + 重启
7. SSE 通知前端

### 效能评分

综合效能分 = 用户评分 25% + Agent 自评 25% + 使用效能 50%
